server:
  port: 9006

spring:
  application:
    name: payment-service-nacos
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848  #注册中心地址


management:
  endpoints:
    web:
      exposure:
        include: "*"